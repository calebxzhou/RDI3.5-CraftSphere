package calebzhou.rdi.craftsphere.misc;

import net.minecraft.client.multiplayer.ClientLevel;
import net.minecraft.world.entity.MobCategory;
import net.minecraft.world.level.NaturalSpawner;

public class RdiEntitySpawner {
    /*private int spawnTick=0;
    public void spawnMob(ClientLevel level){
        MobCategory[] spawningCategories = NaturalSpawner.SPAWNING_CATEGORIES;
        //动物400tick生成一次 怪物20tick生成一次
        for (MobCategory mobCategory : spawningCategories) {
            if (!bl && mobCategory.isFriendly() || !bl2 && !mobCategory.isFriendly() || !bl3 && mobCategory.isPersistent() || !spawnState.canSpawnForCategory(mobCategory, levelChunk.getPos()))
                continue;
            NaturalSpawner.spawnCategoryForChunk(mobCategory, serverLevel, levelChunk, spawnState::canSpawn, spawnState::afterSpawn);
        }
    }*/
}
